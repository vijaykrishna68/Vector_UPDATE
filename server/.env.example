# --- Server ---
# Comma-separated list of allowed origins (your Vercel URL).
CORS_ORIGINS=https://your-app.vercel.app

# Port (most hosts set this automatically)
PORT=4000

# Optional: upload size limit (bytes)
MAX_UPLOAD_BYTES=26214400
